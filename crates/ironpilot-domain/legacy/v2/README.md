# Historical v2 domain evidence

These sources and tests preserve the implemented `StrategyIntent v2` and
deterministic Risk Engine evidence from P1-02/P3-02.

They are intentionally outside `src/` and `tests/`, are not compiled, and are
not exported by `ironpilot-domain`. They must not be referenced by v3 runtime,
Paper, historical harness, or Testnet code.
